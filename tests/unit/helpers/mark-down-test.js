import { markDown } from 'cookbook/helpers/mark-down';
import { module, test } from 'qunit';

module('Unit | Helper | mark down');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = markDown([42]);
  assert.ok(result);
});
