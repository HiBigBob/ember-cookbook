import { classifyElement } from 'cookbook/helpers/classify-element';
import { module, test } from 'qunit';

module('Unit | Helper | classify element');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = classifyElement([42]);
  assert.ok(result);
});
