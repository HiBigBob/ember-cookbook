import { isContains } from 'cookbook/helpers/is-contains';
import { module, test } from 'qunit';

module('Unit | Helper | is contains');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isContains([42]);
  assert.ok(result);
});
